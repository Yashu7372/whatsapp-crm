# ─── Frontend build flags ────────────────────────
# Set to true to run the dashboard against MSW mock handlers
# (no backend needed). Leave false to use the Spring Boot API
# (proxied to http://localhost:8080 by the Vite dev server).
VITE_MOCK=false
